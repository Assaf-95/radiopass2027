# Copy to .env and fill in from sanity.io/manage -> your project -> API.
# Neither value is a secret: the project id appears in every request the
# published site makes. What protects the content is the dataset's ACL and
# Sanity's own login, not the secrecy of these.
SANITY_STUDIO_PROJECT_ID=
SANITY_STUDIO_DATASET=production
