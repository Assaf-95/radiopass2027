{
  "name": "radiopass-studio",
  "private": true,
  "version": "1.0.0",
  "description": "The RadioPass editing interface. Deployed separately from the app.",
  "type": "module",
  "scripts": {
    "dev": "sanity dev",
    "start": "sanity start",
    "build": "sanity build",
    "deploy": "sanity deploy",
    "typegen": "sanity schema extract && sanity typegen generate"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "sanity": "^4.10.0",
    "styled-components": "^6.1.13",
    "@sanity/vision": "^4.10.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
