# Withheld from publication

Files kept for reference but deliberately **not** in `public/`, so they never
reach `dist/` and are never served.

## `msk-p0079.webp`

A screen capture of a third-party commercial anatomy application. It carries
that product's interface, its paywalled label pills, the capturing device's
status bar (time, date, battery), and the answers themselves.

Not ours to republish, and unusable as a question because the answers are
visible. It was never referenced by any question — it was being copied into
the build and served at `/images/revision/msk-p0079.webp` to anyone who
guessed the URL.

Replace with an image you own if this case is wanted.
